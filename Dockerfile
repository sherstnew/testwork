# Используем официальный образ Node.js 18.x на основе Alpine для уменьшения размера
FROM node:18-alpine

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем package.json и package-lock.json (или yarn.lock, если используете yarn)
COPY package*.json ./

# Устанавливаем зависимости
RUN npm ci --only=production

# Копируем остальные файлы приложения
COPY . .

# Копируем .env.local в контейнер (если он существует на хосте)
# Обратите внимание: .env.local обычно не коммитится в репозиторий,
# поэтому его нужно передавать при сборке или монтировать при запуске.
# Здесь предполагается, что вы копируете его вручную или через CI/CD.
COPY .env.local .env.local

# Собираем приложение (если используется next export или нужно собрать статику)
# Для обычного серверного Next.js приложения сборка нужна
RUN npm run build

# Экспонируем порт, который использует Next.js по умолчанию
EXPOSE 3000

# Запускаем приложение
CMD ["npm", "start"]